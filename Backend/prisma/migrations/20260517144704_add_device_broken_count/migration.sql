BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[devices] ADD [brokenCount] INT NOT NULL CONSTRAINT [devices_brokenCount_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
